import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета
  static const Color primary = Color(0xFF9B5CFF); // фиолетовый акцент
  static const Color primaryDark = Color(0xFF7A33FF);

  // Фон
  static const Color background = Color(0xFF0E0E12); // глубокий тёмный
  static const Color card = Color(0xFF1A1A21);

  // Текст
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFBDBDBD);

  // Состояния
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);
}
